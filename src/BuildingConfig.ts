import { BuildingData } from './types';

// 모든 층에 동일하게 적용될 'ㄷ'자 모양의 평면 정보
const floorShape = {
  vertices: [
    // 정점 좌표 (x, y, z) - y는 0으로 고정하여 평면을 정의합니다.
    
    // 왼쪽 기둥 부분 (-15, -10) 에서 (-5, 10) 까지
    -15, 0, -10, // 0
    -5, 0, -10,  // 1
    -5, 0, 10,   // 2
    -15, 0, 10,  // 3

    // 위쪽 팔 부분 (-5, -10) 에서 (15, -5) 까지
    -5, 0, -10,  // 4 (1번과 동일)
    15, 0, -10,  // 5
    15, 0, -5,   // 6
    -5, 0, -5,   // 7

    // 아래쪽 팔 부분 (-5, 5) 에서 (15, 10) 까지
    -5, 0, 5,    // 8
    15, 0, 5,    // 9
    15, 0, 10,   // 10
    -5, 0, 10    // 11 (2번과 동일)
  ],
  faces: [
    // 3개의 사각형을 6개의 삼각형으로 만들어 면을 정의합니다.

    // 왼쪽 기둥 부분
    0, 1, 2,
    0, 2, 3,

    // 위쪽 팔 부분
    4, 5, 6,
    4, 6, 7,

    // 아래쪽 팔 부분
    8, 9, 10,
    8, 10, 11
  ]
};

export const BuildingConfig: BuildingData = {
  floorHeight: 5, // 층간 높이
  floorDimensions: { width: 30, depth: 20 }, // 각 층의 가로, 세로 크기 (기본값)
  totalFloors: 5, // 총 층수 (5로 변경)
  floorConfigs: {
    1: { ...floorShape }, // 1층
    2: { ...floorShape }, // 2층
    3: { ...floorShape }, // 3층
    4: { ...floorShape }, // 4층
    5: { ...floorShape }, // 5층
  }
};